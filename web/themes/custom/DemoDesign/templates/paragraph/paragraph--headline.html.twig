{%
  set headline_attributes = create_attribute({
    'level': headline_type,
    'align': headline_alignment
  })
%}

<div class="container py-5">
  <dc-headline level="{{ headline_attributes.level }}" align="{{ headline_attributes.align }}">
    {{ content|render|striptags }}
  </dc-headline>
</div>
